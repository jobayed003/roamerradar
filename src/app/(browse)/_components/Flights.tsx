'use client';

import { DatePickerWithRange } from '@/components/ui/date-range';

const Flights = () => {
  return (
    <div>
      <DatePickerWithRange />
    </div>
  );
};

export default Flights;
