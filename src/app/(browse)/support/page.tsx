import SupportPage from './_components/SupportPage';

export const metadata = {
  title: 'Support · RoamerRadar',
  description: 'Help with bookings, payments, and your RoamerRadar account.',
};

const Support = () => {
  return <SupportPage />;
};

export default Support;
