import Landing from '@/components/landing';
import Stays from './_components/Stays';

export default function StaysPage() {
  return (
    <Landing img='stays.jpg' heading='Fly, Sleep, Dream'>
      <Stays />
    </Landing>
  );
}
